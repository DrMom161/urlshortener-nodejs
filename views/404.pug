extends layout

block content
  div(class="container")
    h1(class="text-center") Page not found
    div(class="alert alert-danger")
       h3= message
    div(class="text-center")
       a(class="btn btn-primary btn-lg" href="/angular") Home Page
